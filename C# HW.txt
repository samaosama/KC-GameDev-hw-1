using System;
class HelloWorld {
  static void Main() {
      string hero;
      int health=70;
      int damage=health-20;
      health=damage;
      int boost=health+10;
      Console.WriteLine("The damage rate is: "+damage);
      Console.WriteLine("The boost rate is: "+boost);
      if (health>boost){
          Console.WriteLine(health+" is greater than "+boost);
      }
      else if (boost>health){
          Console.WriteLine(boost+" is greater that "+health);
      }
  }
}